mode: "full"

data:
  full:
    ticket: "data/raw/ticket_confirmation.csv"
    delay: "data/raw/delay_prediction.csv"
  sample:
    ticket: "data/sample_data.csv"
    delay: "data/sample_data.csv"
  processed:
    ticket: "data/processed/ticket_cleaned.csv"
    delay: "data/processed/delay_cleaned.csv"

features:
  ticket:
    target: "Confirmation Status"
    drop:
      - "PNR Number"
      - "Booking Date"
      - "Booking Channel"
      - "Current Status"
    use:
      - "Train Number"
      - "Date of Journey"
      - "Class of Travel"
      - "Quota"
      - "Source Station"
      - "Destination Station"
      - "Travel Distance"
      - "Number of Stations"
      - "Travel Time"
      - "Train Type"
      - "Seat Availability"
      - "Holiday or Peak Season"
      - "Waitlist Position"
      - "Number of Passengers"
      - "Age of Passengers"
      - "Special Considerations"

  delay:
    target: "Dealy_min"
    drop:
      - "Train_id"
      - "Train_name"
      - "Sc_arr__time"
      - "Act_arr_time"
    use:
      - "Train_no"
      - "Source"
      - "Destitnation"
      - "Date"
      - "Distance(Km)"
      - "Season"
      - "Run_frequency"

models:
  classifier:
    path: "ml/classifier_model.pkl"
    type: "RandomForest"
  regressor:
    path: "ml/regressor_model.pkl"
    type: "RandomForestRegressor"

reliability:
  weights:
    confirmation: 0.5
    delay: 0.3
    historical: 0.2